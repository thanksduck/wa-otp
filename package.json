{
  "name": "payment",
  "version": "1.0.50",
  "scripts": {
    "build": "bun build ./src/index.ts --outdir --compile --minify ./dist --target bun",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "@elysiajs/bearer": "^1.3.0",
    "@elysiajs/cors": "latest",
    "@elysiajs/swagger": "latest",
    "elysia": "latest"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.0",
    "@types/bun": "latest",
    "typescript": "^5.9.0-beta"
  },
  "module": "src/index.js"
}
